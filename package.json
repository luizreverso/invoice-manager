{
  "type": "module",
  "scripts": {
    "deploy": "sls deploy",
    "openapi": "sls openapi generate -o openapi.json -f json -a 3.0.3 -p postman.json"
  },
  "devDependencies": {
    "esbuild": "^0.19.3",
    "serverless-esbuild": "^1.48.3",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-openapi-documenter": "^0.0.83"
  },
  "dependencies": {
    "aws-sdk": "^2.1464.0",
    "uuid": "^9.0.1"
  }
}
